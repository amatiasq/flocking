#!/bin/bash -e
# amq flocking test — run the Vitest suite once. Extra args pass through to vitest,
# e.g. `amq flocking test --watch` or `amq flocking test cell`.

case "$1" in
  -h|--help) echo "Usage: amq flocking test [vitest args...] — run the test suite"; exit 0;;
esac

exec bun run test "$@"
