#!/bin/bash -e
# amq flocking dev — Vite dev server with HMR. Foreground; Ctrl-C stops it.

case "$1" in
  -h|--help) echo "Usage: amq flocking dev — start the Vite dev server"; exit 0;;
esac

exec bun run dev
