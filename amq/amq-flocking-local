#!/bin/bash -e
# amq flocking local — Vite dev server with HMR. Foreground; Ctrl-C stops it.

case "$1" in
  -h|--help|help) awk 'NR>1 && !/^#/{exit} NR>1{sub(/^# ?/, ""); print}' "$0"; exit 0;;
esac

amq need bun

exec bun run dev
