#!/bin/bash -e
# amq flocking local — Vite dev server with HMR. Foreground; Ctrl-C stops it.

case "$1" in
  -h|--help) echo "Usage: amq flocking local — start the Vite dev server"; exit 0;;
esac

exec bun run dev
