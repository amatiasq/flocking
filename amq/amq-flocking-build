#!/bin/bash -e
# amq flocking build — typecheck (tsc --noEmit) then produce the Vite bundle in dist/.

case "$1" in
  -h|--help) echo "Usage: amq flocking build — typecheck + build the production bundle"; exit 0;;
esac

exec bun run build
